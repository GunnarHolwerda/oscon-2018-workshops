/* eslint-env node, mocha */
// import sessions from './sessions.mjs';

const chai = require('chai');

const { assert } = chai;

describe('sessions', () => {
  describe('new()', () => {
    it('should be awesome', () => {
      assert(true);
    });
  });
});
